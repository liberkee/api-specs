get:
  tags:
   - api
  summary: Retrieve a list of all rental locations from Rentworks system
  description: |
      Retrieve a list of all rental locations from Rentworks system.
  operationId: getLocations
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "./../responses/getLocationsResponse.yaml"
    "400":
      description: |
        Bad Request
    "500":
      description: |
        Internal Server Error
