post:
  tags:
    - api
  summary: Cancel one of your open reservations
  description: |
  operationId: postBookingsExternalBookingIdCancel
  parameters:
    - name: externalBookingId
      in: path
      description: externalBookingId
      required: true
      schema:
        type: "string"
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "./../responses/postBookingsExternalBookingIdCancelResponse.yaml"
    "400":
      description: |
        Bad Request
    "404":
      description: |
        Not Found
    "500":
      description: |
        Internal Server Error
