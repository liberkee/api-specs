post:
  tags:
    - api
  summary: Checkout a booking. Creates an open contract in the external booking system.
  description: |
  operationId: postBookingsExternalBookingIdCheckout
  parameters:
    - name: externalBookingId
      in: path
      description: externalBookingId
      required: true
      schema:
        type: "string"
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "./../responses/postBookingsExternalBookingIdCheckoutResponse.yaml"
    "400":
      description: |
        Bad Request
    "404":
      description: |
        Not Found
    "500":
      description: |
        Internal Server Error
