post:
  tags:
    - api
  summary: Checkout a booking. Creates an open contract in the external booking system.
  description: |
  operationId: postBookingsRentalBookingIdCheckout
  parameters:
    - in: header
      name: x-api-username
      required: true
      schema:
        type: string
    - in: header
      name: x-api-password
      required: true
      schema:
        type: string
    - in: header    
      name: x-api-url
      required: true
      schema:
        type: string
    - name: rentalBookingId
      in: path
      description: rentalBookingId
      required: true
      schema:
        type: string
  requestBody:
    x-name: body
    required: true
    content:
      application/json:
        schema:
          $ref: "./../requests/postBookingsRentalBookingIdCheckoutRequest.yaml"
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "./../responses/postBookingsRentalBookingIdCheckoutResponse.yaml"
    "400":
      description: |
        Bad Request
    "404":
      description: |
        Not Found
    "500":
      description: |
        Internal Server Error
