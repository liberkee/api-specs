get:
  tags: 
   - approved
  summary: Get master data of all vehicles.
  description: | 
  operationId: vehicles/masterData
  parameters:
    - name: Accept
      in: header
      description: "Accept: application/json"
      type: string
      default: application/json
    - name: Accept-Language
      in: header
      description: LanguageCode referred to ISO 639 
      type: string
      default: en-EN
    - name: tenantId
      in: query
      type: string
  responses:
    Response body (200 OK):
      $ref: "../../swagger.yaml#definitions/vehiclesMasterData"