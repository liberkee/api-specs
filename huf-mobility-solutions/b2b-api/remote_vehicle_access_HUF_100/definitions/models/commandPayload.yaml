description: ""
type: "object"
properties: 
  externalVehicleRef:
    description: "Reference to the vehicle object as used in the external booking system"
    type: "string"
    minLength: 1
  tenantId:
    description: "Optional ID specifying the tenant the vehicle belongs to"
    type: "string"
    minLength: 1
  commands:
    type: "array"
    minItems: 1
    maxItems: 1
    items:
      $ref: "../../swagger.yaml#definitions/command"
required:
  - externalVehicleRef
  - commands