type: "object"
properties:
  vehicleAccessGrantId:
    description: "The ID that was used to notify of the corresponding grant"
    type: "string"
    minLength: 1
    format: "UUID (RFC4122)"

required:
  - vehicleAccessGrantId