post:
  tags:
   - api
  summary: Search for quotes of vehicle classes
  description: |
  operationId: postQuote
  requestBody:
    x-name: body
    required: true
    content:
      application/json:
        schema:
          $ref: "./../requests/postQuoteRequest.yaml"
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "./../responses/postQuoteResponse.yaml"
    "400":
      description: |
        Bad Request
        | reason | info |
        | :---         |     :---      |
        | time issue | end time must be greater than start time |
        | time issue | start or end time can´t be in the past |
    "500":
      description: |
        Internal Server Error
