post:
  tags:
    - api
  summary: Checkin a booking. This will close the contract in external booking system.
  description: |
  operationId: postBookingsRentalBookingIdCheckin
  parameters:
    - in: header
      name: x-api-username
      required: true
      schema:
        type: string
    - in: header
      name: x-api-password
      required: true
      schema:
        type: string
    - in: header    
      name: x-api-url
      required: true
      schema:
        type: string
    - name: rentalBookingId
      in: path
      description: rentalBookingId
      required: true
      schema:
        type: string
  requestBody:
    x-name: body
    required: false
    content:
      application/json:
        schema:
          $ref: "./../requests/postBookingsRentalBookingIdCheckinRequest.yaml"
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "./../responses/postBookingsRentalBookingIdCheckinResponse.yaml"
    "400":
      description: |
        Bad Request
    "404":
      description: |
        Not Found
    "500":
      description: |
        Internal Server Error
