get:
  tags:
   - api
  summary: Retrieve a list of all rental locations from the external booking system
  description: |
      Retrieve a list of all rental locations from the external booking system.
  operationId: getLocations
  parameters:
    - in: header
      name: x-api-username
      required: true
      schema:
        type: string
    - in: header
      name: x-api-password
      required: true
      schema:
        type: string
    - in: header    
      name: x-api-url
      required: true
      schema:
        type: string
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "./../responses/getLocationsResponse.yaml"
    "400":
      description: |
        Bad Request
    "500":
      description: |
        Internal Server Error
